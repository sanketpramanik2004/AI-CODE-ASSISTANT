# Sample Upload Repo

This small repo is intentionally buggy so the analyzer has something meaningful to detect.

Expected issues include:
- binary search boundary mistakes
- possible infinite loop in binary search
- `==` used for Java string comparison
